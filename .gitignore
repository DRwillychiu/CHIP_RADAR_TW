# v3.31.10: chip_radar_v2.db 69MB 超 GitHub 50MB 警告, 不進 git
data/chip_radar_v2.db
data/chip_radar_v2.db-wal
data/chip_radar_v2.db-shm
__pycache__/
*.pyc

# v3.41.0 C1: event log + fetch quota 不入 repo (運行時資料)
logs/
data/fetch_quota.json

# 本機設定 (含密碼，不上傳)
.env

# Windows 本機執行輸出 (不上傳)
local_data/
scripts/.server.pid
scripts/.certs/
.venv/

# v3.74.1: 借跑 disposal-watch 的工作副本 (scripts/push_disposal_telegram.py)
# 正常會在 finally 清掉,但 crash / --keep 時會殘留;排程用 local_data 已被
# 上面涵蓋,這裡是給手動執行 (DATA_DIR=data) 兜底
.disposal_watch_run/
.disposal_watch.zip
.tg_disposal_push
